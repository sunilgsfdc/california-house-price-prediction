name: Hello Workflow

on: [push]

jobs:
  hello-job:
    runs-on: ubuntu-latest
    
    steps:
      - name: Print Hello
        run: echo "Hello, GitHub Actions"
        
      - name: Print Date
        run: date